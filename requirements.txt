# Python 3.14 compatibility note: many libs only ship 3.14 wheels in their latest
# minor versions, so most pins are lower-bounds rather than equality.

fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.18
sqlalchemy>=2.0.36
alembic>=1.14
psycopg[binary]>=3.2       # Postgres driver (DATABASE_URL=postgresql+psycopg://...)
pydantic>=2.13
pydantic-settings>=2.6
pandas>=3.0.0
openpyxl>=3.1
xlsxwriter>=3.2
python-dateutil>=2.9

# Outbound HTTP for the SAP inventory feed + scheduled auto-sync.
# httpx is the client (also used by the test suite's TestClient); apscheduler
# runs the in-process weekday cron; tzdata ships the IANA zone database so
# ZoneInfo("America/Los_Angeles") resolves on the slim Docker image.
httpx>=0.28
apscheduler>=3.10,<4
tzdata>=2024.1

# PDF generation (invoices). Upper-bounded against major version bumps
# because invoices are customer-visible documents and rendering behavior
# needs to stay stable. Requires system libs: libpango, libcairo,
# libharfbuzz, libffi (see Dockerfile).
weasyprint>=63.1,<64

# Auth (Phase 1: per-user logins)
bcrypt>=4.0                # direct API; passlib's wrapper is broken on bcrypt 4.x
itsdangerous>=2.2          # Starlette SessionMiddleware uses this for signing
authlib>=1.3               # Google OAuth ("Sign in with Google"); OIDC discovery

# dev
pytest>=8.3
pytest-cov>=6.0

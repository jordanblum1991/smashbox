# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# App data
data/*.db
data/*.db-journal
uploads/*
!uploads/.gitkeep
exports/*
!exports/.gitkeep

# Node / Tailwind build
node_modules/
# Compiled Tailwind stylesheet — a build artifact. Production rebuilds it in
# the Dockerfile's Node stage on every deploy; locally run `npm run css`
# (or `npm run css:watch`) once before starting uvicorn.
app/static/css/tailwind.css

# Env
.env
.env.local

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

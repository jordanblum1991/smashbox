"""Demand planning module — velocity, replenishment, and forecasting services.

These are pure functions that take a SQLAlchemy session and return data
classes; no I/O, no UI. The reports + routes consume them.
"""
